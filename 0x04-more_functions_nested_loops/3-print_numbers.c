#include "main.h"

/**
 * print_numbers - entry point
 *
 *
 *
 *Return: Always 0 (Success)
*/

void print_numbers(void)
{
        int num;

        for (num = 0; c <= 9; c++)
        {
                _putchar(num + 48);
        }
        _putchar('\n');
}
