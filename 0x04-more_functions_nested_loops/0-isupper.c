#include "main.h"

/**
 * _isupper - entry point
 *
 * @c: function 
 *
 *Return: Always 1 if upper else 0 (Success)
*/

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
		return (100);
	else
		return (0);
}
